# 토스증권 API 기반 주식 자동매매 시스템 PRD

## 1. 프로젝트 개요

### 1.1 프로젝트명

토스증권 API 기반 주식 자동매매 시스템

### 1.2 목적

토스증권 Open API와 Python을 활용하여 주식 시세 조회, 계좌 조회, 매매
조건 판단, 주문 관리 및 거래 기록 저장 기능을 갖춘 자동매매 시스템을
개발한다.

초기 개발 단계에서는 실제 주문을 실행하지 않고 조회 및 분석 기능을
중심으로 검증한다.

### 1.3 개발 방향

-   Python 기반 API 서버 구축
-   FastAPI를 이용한 백엔드 개발
-   토스증권 Open API 연동
-   PostgreSQL을 활용한 거래 데이터 저장
-   웹 대시보드를 통한 시스템 상태 및 거래 내역 확인
-   실제 투자 전 모의·검증 단계 운영

------------------------------------------------------------------------

## 2. 시스템 구성

``` text
[웹 화면]
    |
    v
[FastAPI 서버]
    |
    +-- 주식 조회
    +-- 계좌 조회
    +-- 매매 전략 판단
    +-- 주문 관리
    |
    v
[토스증권 Open API]
    |
    v
[거래 결과]
    |
    v
[PostgreSQL]
```

------------------------------------------------------------------------

## 3. 주요 기능 요구사항

### 3.1 API 인증

-   토스증권 API 인증 기능을 구현한다.
-   `client_id`와 `client_secret`을 이용하여 액세스 토큰을 발급받는다.
-   발급받은 액세스 토큰을 API 요청에 사용한다.
-   계좌 관련 API 요청 시 필요한 계좌 식별 정보와 헤더를 관리한다.
-   인증 정보는 환경변수 또는 `.env` 파일로 관리한다.
-   인증 정보가 GitHub에 노출되지 않도록 한다.

### 3.2 주식 정보 조회

-   종목 코드 또는 종목명을 기준으로 주식 정보를 조회한다.
-   현재가 및 필요한 시세 정보를 가져온다.
-   API 응답 성공 여부와 오류 메시지를 처리한다.
-   초기 테스트는 실제 주문 없이 조회 기능만 수행한다.

### 3.3 계좌 조회

-   계좌 잔고를 조회한다.
-   보유 종목 정보를 조회한다.
-   주문 가능 금액을 확인한다.
-   계좌 조회 결과를 화면에 표시할 수 있도록 구성한다.

### 3.4 매매 전략

-   사용자가 매수·매도 조건을 설정할 수 있도록 설계한다.
-   주식 가격과 설정 조건을 비교한다.
-   조건 충족 시 매수 또는 매도 신호를 생성한다.
-   초기 단계에서는 주문 실행 대신 신호만 생성하는 분석 모드를 제공한다.
-   전략별 실행 결과를 기록한다.

### 3.5 주문 관리

-   매수 주문 요청 기능을 구현한다.
-   매도 주문 요청 기능을 구현한다.
-   주문 전 종목, 수량, 가격, 주문 가능 금액을 확인한다.
-   주문 결과와 오류를 처리한다.
-   초기 개발 및 테스트에서는 실제 주문을 비활성화한다.
-   실제 주문 기능은 충분한 테스트와 사용자 확인 이후 별도로 활성화한다.

### 3.6 거래 기록 저장

-   주문 요청 기록을 저장한다.
-   주문 결과와 체결 상태를 저장한다.
-   종목 코드, 주문 종류, 수량, 가격, 시간 등의 정보를 관리한다.
-   PostgreSQL에 거래 내역을 저장한다.

### 3.7 웹 대시보드

-   보유 종목을 표시한다.
-   현재가 및 계좌 정보를 표시한다.
-   매매 신호를 표시한다.
-   주문 및 체결 내역을 표시한다.
-   자동매매 시스템의 실행 상태를 표시한다.
-   실제 주문 활성화 여부를 명확히 표시한다.

------------------------------------------------------------------------

## 4. 기술 스택

  구분                기술
  ------------------- -------------------------
  프로그래밍 언어     Python
  백엔드 프레임워크   FastAPI
  증권 API            토스증권 Open API
  데이터베이스        PostgreSQL
  데이터베이스 도구   DBeaver
  API 테스트          Swagger UI 또는 Postman
  형상 관리           Git / GitHub
  환경변수 관리       `.env`, `python-dotenv`

------------------------------------------------------------------------

## 5. 초기 프로젝트 파일 구조

``` text
stock_auto_trading/
|
+-- main.py
+-- config.py
+-- toss_api.py
+-- token_test.py
+-- requirements.txt
+-- .env
+-- .gitignore
|
+-- services/
|   +-- market_service.py
|   +-- account_service.py
|   +-- order_service.py
|   +-- strategy_service.py
|
+-- database/
|   +-- connection.py
|   +-- models.py
|   +-- repository.py
|
+-- README.md
+-- PRD.md
```

------------------------------------------------------------------------

## 6. 1차 개발 범위(MVP)

### 6.1 1단계: 인증 테스트

-   필요한 Python 패키지 설치
-   환경변수 설정
-   토스증권 액세스 토큰 발급
-   응답 상태 코드 및 JSON 확인
-   인증 오류 처리

설치 패키지 예시:

``` bash
pip install requests python-dotenv fastapi uvicorn
```

### 6.2 2단계: 주식 현재가 조회

-   인증 토큰을 이용한 API 요청
-   테스트 종목의 현재가 조회
-   응답 데이터 출력
-   API 오류 처리

### 6.3 3단계: 계좌 조회

-   계좌 정보 조회
-   잔고 및 보유 종목 확인
-   주문 가능 금액 확인

### 6.4 4단계: 매매 조건 판단

-   기준 가격 입력
-   매수·매도 조건 설정
-   조건 충족 여부 확인
-   주문 없이 매매 신호 출력

### 6.5 5단계: 데이터베이스 연결

-   PostgreSQL 연결
-   시세 데이터 저장
-   매매 신호 저장
-   주문 기록 테이블 설계

### 6.6 6단계: 주문 기능 검증

-   주문 요청 데이터 검증
-   실제 주문 전 안전장치 구현
-   테스트 환경 또는 모의 검증
-   오류 및 중복 주문 방지
-   사용자 확인 후 실제 주문 기능 검토

------------------------------------------------------------------------

## 7. 보안 요구사항

-   `client_secret`, 액세스 토큰, 계좌 정보는 코드에 직접 작성하지
    않는다.
-   `.env` 파일을 GitHub에 업로드하지 않는다.
-   `.gitignore`에 `.env`를 등록한다.
-   API 키와 토큰을 로그에 출력하지 않는다.
-   GitHub에 비밀정보가 업로드된 경우 즉시 키를 폐기하거나 재발급한다.
-   실제 주문 기능은 기본적으로 비활성화한다.
-   주문 수량과 금액에 제한을 둔다.
-   네트워크 오류 및 API 오류 발생 시 주문 재시도를 신중하게 처리한다.

예시 `.gitignore`:

``` gitignore
.env
__pycache__/
*.pyc
.venv/
```

------------------------------------------------------------------------

## 8. 비기능 요구사항

### 8.1 안정성

-   API 응답 오류를 처리한다.
-   네트워크 지연 및 연결 실패를 처리한다.
-   중복 주문을 방지한다.
-   프로그램 재실행 시 상태를 확인할 수 있도록 한다.

### 8.2 추적성

-   API 요청 시간과 결과를 기록한다.
-   매매 신호 발생 이유를 기록한다.
-   주문 상태 변경을 저장한다.

### 8.3 사용성

-   초보자도 확인할 수 있는 단순한 화면을 제공한다.
-   실제 주문 활성화 여부를 명확히 표시한다.
-   오류 메시지를 이해하기 쉬운 형태로 표시한다.

------------------------------------------------------------------------

## 9. 예상 API 연동 흐름

``` text
1. 환경변수에서 client_id와 client_secret 읽기
2. 토스증권 인증 API 호출
3. access_token 발급
4. 인증 헤더 구성
5. 현재가 또는 계좌 조회 API 호출
6. 응답 데이터 검증
7. 필요한 데이터 PostgreSQL 저장
8. 매매 조건 판단
9. 분석 모드에서는 신호만 출력
10. 검증 완료 후 주문 기능 별도 적용
```

------------------------------------------------------------------------

## 10. 성공 기준

-   토스증권 API 인증 토큰을 정상적으로 발급받을 수 있다.
-   테스트 종목의 현재가를 조회할 수 있다.
-   계좌 및 잔고 정보를 조회할 수 있다.
-   설정한 조건에 따라 매수·매도 신호를 생성할 수 있다.
-   거래 및 신호 데이터를 PostgreSQL에 저장할 수 있다.
-   API 오류와 네트워크 오류를 처리할 수 있다.
-   민감한 인증 정보가 GitHub에 노출되지 않는다.
-   실제 주문 실행 여부를 명확히 통제할 수 있다.

------------------------------------------------------------------------

## 11. 위험 및 제한사항

-   주식 자동매매는 원금 손실 가능성이 있다.
-   API 제공 범위, 요청 형식, 호출 제한 및 주문 가능 조건은 토스증권
    공식 문서를 기준으로 확인해야 한다.
-   실제 주문 전 충분한 테스트가 필요하다.
-   API 오류, 네트워크 오류, 가격 변동 및 체결 지연이 발생할 수 있다.
-   자동매매 전략이 수익을 보장하지 않는다.
-   초기 개발 단계에서는 조회 및 분석 모드를 우선한다.

------------------------------------------------------------------------

## 12. 다음 작업 우선순위

1.  토스증권 공식 문서에서 인증 API 요청 형식 확인
2.  `.env` 및 `.gitignore` 구성
3.  액세스 토큰 발급 테스트
4.  현재가 조회 API 테스트
5.  계좌 조회 API 테스트
6.  매매 신호 생성 기능 구현
7.  PostgreSQL 테이블 설계
8.  FastAPI 엔드포인트 구현
9.  웹 대시보드 구성
10. 안전장치 검토 후 주문 기능 검증



